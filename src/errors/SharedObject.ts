/**
 * A simple shim for SharedObject from expo-modules-core to fix import errors
 */
export class SharedObject {
  constructor() {
    // This is just a shim class
  }
}

export default SharedObject; 